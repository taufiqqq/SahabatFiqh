## Packages
framer-motion | Smooth animations for the character and UI transitions
date-fns | Formatting dates in conversation history
clsx | Conditional class merging
tailwind-merge | Class merging utility

## Notes
- SSE streaming required for chat messages via POST /api/conversations/:id/messages
- Character animation state (idle/talking) depends on SSE stream status
- Islamic theme colors: Primary Green (#2E8B57), Secondary Yellow (#E6C35C)
- Fonts: 'Amiri' (Display/Serif) and 'DM Sans' (Body/Sans)
